Run insert_sensor_pythonscript in order to insert all the insert_sensor xml files within
the data folder specified in the script. The data folder path and folder has to be modified depending on
where the xmls are stored.

 Also the URL has to be modiefied depending on which SOS instance the sensors should be inserted.